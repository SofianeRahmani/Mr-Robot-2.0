<UserControl x:Class="RecordETL.Views.ExcelView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:RecordETL.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
   <Grid>
      <Grid.RowDefinitions>
         <RowDefinition Height="Auto"/>
         <RowDefinition />
      </Grid.RowDefinitions>


      <Button x:Name="DropArea" Content="{Binding ExcelPath, UpdateSourceTrigger=PropertyChanged}">
      </Button>

      <DataGrid Grid.Row="1" ItemsSource="{Binding ExcelData}" AutoGenerateColumns="True" />
   </Grid>
</UserControl>
